---
title: Estimating Causal Effects from Data Generated by Stochastic Algorithms
title_zh: 基于随机算法生成的数据估计因果效应
authors: "Susan Athey, Guido Imbens, Zoe Ji"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05792v1"
tags: ["query:causal-infer"]
score: 9.0
evidence: 从推荐系统的观测数据中估计内容特征的因果效应
tldr: 在推荐系统、聊天机器人等由随机算法生成内容的情境下，内容特征（如回复风格、视频类型）对用户参与度的因果效应是优化算法的关键，但因内容高度定制化且维度纷繁，从观察数据中估计此类效应极具挑战。本文提出一种新颖的识别策略，仅需记录每个用户至少一个未曝光项目及其与曝光项目的概率比值，即可利用算法中已有的随机性。理论证明，即使存在未观测的、同时影响用户偏好和被考虑项目对的混杂因素，内容特征的因果效应仍然可识别。基于此构建的估计器不同于以往仅依赖曝光数据的方法，其构造更充分利用随机化信息，为大规模内容平台的效应评估提供了一条高效、低成本的路径。该方法有望广泛应用于推荐、对话系统等领域，推动基于因果的内容优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05792v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1180, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05792v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1178, \"height\": 760, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05792v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1181, \"height\": 752, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05792v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1637, \"height\": 643, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05792v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1114, \"height\": 1375, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05792v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1634, \"height\": 549, \"label\": \"Figure\"}]"
motivation: 个性化推荐使内容曝光与用户特征紧密相关，且内容高维多变，传统观察性方法难以估计特征因果效应。
method: 利用算法随机性，记录未曝光项目标识及其相对曝光概率比，构建新估计器实现特征效应的非参数识别。
result: 理论证明，在未观测混杂影响用户偏好和项目配对时因果效应仍可识别；估计器构造和数据利用方式不同于以往。
conclusion: 该方法仅需常规日志数据，即可在随机内容投放中实现特征因果效应的可靠推断，为内容平台提供高效评估工具。
---

## 摘要
推荐系统和聊天机器人向用户展示内容，通常使用基于用户特征或情境选择内容的随机算法。内容示例包括聊天回复、视频或可供购买的商品。科学家和应用开发者通常关注内容特征是否会提升用户参与度等结果。对此类因果效应的估计可以指导内容提供者生成强调理想特征的内容。然而，在内容库庞大或内容针对特定用户唯一生成的情境下，利用观测数据学习内容特征的因果效应可能很困难，因为用户看到的内容是量身定制的，且内容在许多维度上存在差异。本文提出了一种新方法，当决定用户曝光的算法包含一定随机性，并且为每个用户记录了以下两个额外数据元素时，利用观测数据估计内容特征的影响：(i) 至少一个本可以展现给用户但并未展现的物品（未曝光物品）的身份；(ii) 未曝光物品被展示的概率与已曝光物品被展示的概率之比的估计值。我们证明，即使存在同时影响用户偏好和所考量物品对（曝光和未曝光）身份的未观测混杂因素，该情境下特征的因果效应仍可被识别。我们的估计量在所用数据以及估计量的构造方式方面均不同于先前的方法。

## Abstract
Recommendation systems and chatbots present content to users, typically using stochastic algorithms that select the content based on user characteristics or context. Examples of content include chat responses, videos, or items available for purchase. Scientists and application developers are often interested in whether characteristics of content increase outcomes such as user engagement. Estimates of such causal effects may guide content providers to generate content that emphasize desirable features. However, in settings with a large content library or where content is generated uniquely for a given user, it can be difficult to use observational data to learn the causal effect of content features, because the content a user sees is tailored to that user, and because content varies in many dimensions. This paper proposes a new method for estimating the impact of content features using observational data, when the algorithm that determines user exposure incorporates some randomization, and when two additional data elements are logged for each user: $(i)$ the identity of at least one item that could have been exposed to the user, but was not (the unexposed item); $(ii)$ an estimate of the ratio of the probability that the unexposed item would have been shown to the probability that the exposed item was shown. We show that causal effects of features are identified in this setting, even in the presence of unobserved confounders that affect both user preferences and the identity of the considered pair of items (exposed and unexposed). Our estimator differs from prior approaches in terms of what data is used and how the estimator is constructed.

---

## 论文详细总结（自动生成）

好的，我将根据您提供的论文内容，以中文、Markdown格式，生成一份结构化、深入且客观的总结。

### 1. 论文的核心问题与整体含义

这篇论文的核心在于解决一个在现代AI推荐与内容生成系统中普遍存在的因果推断难题：如何从高度个性化、内容维度极其复杂的观测数据中，可靠地估计内容特征（如文本的友好度、图片是否包含微笑等）对用户结果（如点击率、参与度）的因果效应。

-   **研究动机与背景**：
    -   **个性化带来的挑战**：现代推荐系统和生成式AI（如大语言模型LLM）会根据用户特征和查询上下文，为用户展示高度定制化的内容。这使得不同用户看到的内容截然不同，导致用户偏好、查询情境等成为混杂因素。例如，喜欢做作业的学生可能看到包含更多事实信息的内容，且参与度更高，但这并不能说明对所有用户增加事实信息都会提升参与度。
    -   **内容高维度的挑战**：内容在众多维度上变化，目标特征（如事实性）常与其他无法完全观测或控制的维度（如共情、风格）相关联。这意味着改变某个单一特征的效应，实际上捆绑了与之相关的其他特征变动的效应。
    -   **现有方法的局限**：传统的AB测试成本高，且因内容维度过高而难以穷尽所有组合。基于无混淆假设的观测数据方法（如倾向性得分匹配）难以完整测量并控制所有混杂因素。
    -   **核心思想**：论文提出，现代AI系统本身的**随机性**就是天然的“微实验”。每次算法根据用户情境随机选择一个或多个内容进行候选，再从中随机选择一个曝光，这一过程就在用户层面生成了一个实验。只需额外记录两个信息——(1)未曝光候选项的特征；(2)已曝光项相对于未曝光项的概率——我们就能将这些常规的交互日志转化为可以识别因果效应的数据，即“已记录的反事实曝光” (Logged Counterfactual Exposures, LCE) 数据。

### 2. 论文提出的方法论

论文的核心方法论是定义了一个名为“条件-处理-平均因果效应” (Conditional Treatment-Averaged Causal Effects, C-TACE) 的估计量，并证明了在LCE数据下，该估计量可以被识别，即使存在未观测混杂因素。

-   **核心思想与框架**：
    -   **微观实验设定**：每个单元（用户）$i$ 都有一个特定的处理集合 $\mathcal{A}_i$（例如，LLM生成的两个候选回复），系统会从该集合中随机选择一个作为实际曝光项 $A_i$，另一个则为未曝光项 $A_i^N$。这构成了一个样本量为1的实验。
    -   **区分处理与特征**：明确区分了“处理项” $A_i$（被曝光的那个独特内容）和“处理特征” $V_i = f(A_i)$（该内容可测量的特征，如“友好度”）。因果关系是在处理项层面定义的，但由于处理项可能唯一，我们只能通过特征层面来汇总和分析。

-   **关键技术细节与识别策略**：
    -   **C-TACE 估计量**：该估计量是对单元级因果效应 $\tau_i = Y_i(A_i) - Y_i(A_i^N)$ 的一种加权平均，权重取决于选定的“处理特征函数” (Treatment Characterizing Function, TCF) $h(v, v')$。TCF是一个反对称函数，例如 $h(v, v') = v - v'$，用于定义特征差异的方向。
    -   **核心识别定理**：在给定被曝光和未曝光项目对 $(V_i, V_i^N)$ 及其特征差异 $h(V_i, V_i^N)$ 的条件下，通过计算特征差异为正（$h > 0$）的单元组与特征差异为负（$h < 0$）的单元组的平均结果 $Y_i$ 之差，即可无偏地识别出 C-TACE。
        $$\tau_h(\delta) = \mathbb{E}[Y_i | h(V_i, V_i^N) = \delta] - \mathbb{E}[Y_i | h(V_i, V_i^N) = -\delta]$$
    -   **伪实验变换**：通过TCF，可以将观察到的数据 $(\text{Y}_i, V_i, V_i^N)$ 映射到一个“伪实验”框架。在这个伪实验中，处理变量 $D_i^h$ 是三元变量（$-1, 0, 1$），分别对应 $\text{TCF}$ 为负、零、正。通过构造，该伪实验中的处理分配 $D_i^h$ 在给定 $D_i^h \neq 0$ 的条件下，与潜在结果是独立的。
    -   **关键数据处理**：明确要求剔除 $D_i^h = 0$（即 $h(V_i, V_i^N) = 0$）的单元，因为在这些单元中，被曝光和未被曝光项目的特征没有差异，不包含与TCF相关的因果信息，如果保留会稀释或扭曲效应估计。
    -   **处理未知或非等概率分配**：当分配概率未知或不相等时，方法可以扩展。论文提出，对于未知概率，可以通过多次“重放”随机算法来估计。特别地，一种称为“逆采样”的序贯设计可以得到概率倒数的无偏估计，从而构造一个无偏的伪处理项 $\hat{D}_i^h$，尽管这会带来方差代价。

### 3. 实验设计

本论文是方法论导向的，**没有使用真实世界的大规模数据集进行实证分析**，而是通过一个线性模型的模拟例子，清晰地演示和验证了其方法与其它替代方法在理论上的差异和优势。

-   **模拟场景**：一个关于P2P贷款申请的活动场景，LLM为每个潜在贷款人从两个候选广告词中选择一个展示。内容特征包含“具体性”和“故事性”，用户特征包含对两者的偏好。

-   **对比的基准方法**：
    1.  **朴素回归**：仅用实际曝光项的特征$V_{i,c}$对结果$Y_i$回归。
    2.  **控制协变量的回归（无混淆假设分析）**：将用户特征$X_i$作为协变量加入上述回归。
    3.  **负样本比较**：使用未曝光的候选项作为“负样本”构建损失或进行对比。
    4.  **论文提出的C-TACE估计量**：进行配对内对比并剔除平局样本。

-   **模拟验证的关键点**：
    -   **选择偏差抵抗力**：模拟显示，当推荐系统的排序决策与用户偏好（混杂因素）关联增强时，朴素回归的估计值会混杂偏好项的影响而偏离真实值。而C-TACE估计量和控制了所有可观测协变量的回归则能保持稳健。
    -   **未观测混杂抵抗力**：进一步模拟表明，当推荐系统基于一个**未被观测到**的用户特征进行排序时，协变量回归的估计值会偏航，而利用了配对内随机化的C-TACE估计量依然是无偏的。
    -   **平局处理的必要性**：模拟直观地展示了不恰当处理平局（特征无差异的配对）会如何稀释效应甚至导致符号反转。

### 4. 资源与算力

**本论文未提及具体使用的算力资源、GPU型号、数量或训练时长**。因为其核心贡献在于统计识别框架和估计量的理论推导，并用一个简化的、可解析计算的线性模拟作为演示，不涉及大规模数据的深度学习模型训练，因此对算力没有要求。

### 5. 实验数量与充分性

-   **实验数量**：论文主要围绕**一个核心的线性模拟示例**展开，通过系统地改变参数（如推荐系统排序的强度、是否存在未观测混杂、特征之间是否关联等）来生成多组对比图表。这些可以被视为多组消融实验。
-   **实验充分性与客观性**：
    -   **充分性**：作为一种方法论文，其设计的模拟实验是**充分且精准的**。它不是要证明该方法在某个具体任务上的性能指标超过其他模型，而是要直观、透明地揭示不同统计方法背后的**因果识别机理**。每个图表都专门设计用于解释一个核心概念（如：剔除平局的重要性、未观测混杂的影响）。
    -   **客观公平性**：对比是公平的。所有方法都基于完全相同的数据生成过程，并比较了它们在处理选择偏差和未观测混杂时的理论极限（概率极限）。这比在特定真实数据集上比较精度更有说服力，因为真实数据中没有可参照的“真实因果效应”。

### 6. 论文的主要结论与发现

1.  **因果效应可识别，无需无混杂假设**：通过记录和使用LCE数据（未曝光项的特征及其相对曝光概率），可以识别内容特征的因果效应（C-TACE），**无需满足传统的“无混杂假设”**，即使存在同时影响用户偏好和被考量物品对的未观测混杂因素。
2.  **明确区分了新的估计量**：C-TACE估计量不同于以往的方法，它本质上是基于配对内的随机化，消除了不随时间变化的、用户层面的所有可观测和不可观测的混杂。
3.  **LCE数据具有高度实用性**：论文论证了LCE数据可以通过多种途径大规模获取：大型语言模型(LLM)的API日志概率、推荐系统的多阶段候选集、或者简单地通过离线编写脚本“重放”相同的随机算法。这使得该方法具备广阔的应用前景。
4.  **正确剔除平局项至关重要**：必须剔除那些特征无差异的候选对，否则会稀释效应或引入偏差。
5.  **提供了评估LLM的新范式**：论文提出可以利用其框架间接比较不同大语言模型的表现，而无需要求用户直接对多个长回复进行排序，只需随机展示其中一个回复并记录其日志概率和结果即可。

### 7. 优点

-   **方法论创新性强**：提出了LCE数据和C-TACE这两个核心概念，巧妙地利用了算法中固有的随机性来解决长期存在的个人化场景下的因果推断难题。
-   **假设条件弱，识别力强**：放松了因果推断中普遍使用的无混淆假设，能够在存在强未观测混杂时获得无偏的因果效应估计，是一个显著的进步。
-   **实用价值高**：明确指出了该框架在LLM和现代推荐系统中的实际落地路径，所需的额外数据获取成本相对较低，且不影响线上用户体验。
-   **解释清晰透彻**：通过“微观实验”、“伪实验”等形象的比喻和一个贯穿始终的线性模拟例子，将复杂的因果机制和估计量解释得非常清晰。

### 8. 不足与局限

-   **实验验证的局限**：论文缺乏在真实、复杂的大规模数据集上的实证验证。模拟例子虽然逻辑清晰，但无法充分展示该方法在处理高维噪音、非线性和动态变化环境时的稳健性和统计效率。
-   **假设条件的限制**：核心假设“可复现的随机化”（Assumption 2）在实践中可能受到挑战。要求被重放或记录的算法版本、提示词、温度等参数必须与生产环境完全一致，而线上推荐系统更新极其频繁，可能难以满足。
-   **估计的解释性**：C-TACE估计的是随着算法输出而“捆绑”在一起的所有特征的效应，而不是孤立地改变单一特征的效应。虽然论文论证了这对平台而言是政策相关的量，但对于想要理解底层消费心理的科学家来说，可能解释上不够纯粹。
-   **多重处理的交互效应**：对于多个目标特征，论文指出由于只有一个随机化种子，我们无法识别一个特征在另一个特征确定的子群中的因果效应（如：不能仅在“故事性”高的内容里识别“具体性”的效应）。这使得端到端的因果效应分解和交互效应的探索受到限制。
-   **数值处理的简化**：论文假设特征$V_i$是离散的（甚至主要是二元的），而对于文本情感、主题等连续或高维嵌入向量的处理，定义TCF $h(v,v')$和剔除平局项可能更复杂。

（完）
